// slice method for str
let name = "SyedImtiyazAli";
console.log(name.slice(4,11))

// concat str
let firstName = 'Syed Imtiyaz';
let lastName = 'Ali';
console.log(firstName.concat(` ${lastName}`))

// replace str, this method have two parameter for doin+g the task first is (searchValue, repalceValue)
let myStr = `I love to code in C++`;
console.log(myStr.replace(`C++`,`Java`));

// chatAt - gives the element on the index
let text = "Imtiyaz";
console.log(text.charAt(0));
